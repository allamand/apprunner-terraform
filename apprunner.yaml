version: 1.0
runtime: python3
build:
  commands:
    build:
      - cd votingapp && pip install -r requirements.txt
run:
  command: python votingapp/app.py
  network:
    port: 8080
  env:
    - name: DDB_AWS_REGION
      value: 'eu-west-1'
